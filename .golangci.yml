linters:
  enable-all: true
  disable:
    - funlen
    - lll

issues:
  exclude-rules:
    - linters:
        - scopelint
      path: "_test\\.go"
    - linters:
        - gochecknoglobals
      path: internal/generate-testdata